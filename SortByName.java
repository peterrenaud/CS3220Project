import java.util.Comparator;

class SortByName implements Comparator<ElectedOfficials> {
  //  public int compare(ElectedOfficials a, ElectedOfficials b) {
  // return a.getName() - b.getName(); // this is used when comparing different names of elected officials to sort them // getName method needs to be implemented
    // this is a template to instantiate the other sort functions
  }
}
